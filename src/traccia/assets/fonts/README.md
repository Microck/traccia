# Traccia Viewer Fonts

This directory contains the normalized font subset used by the static viewer
export. The exporter copies these files into generated viewer bundles by
default, so a clean checkout renders with the same typography.

The ignored `private/font-assets/` directory can still hold original foundry
packages or alternate local font sources. Do not replace the committed subset
without confirming the font license permits the intended repository and deploy
usage.
